export class ExportedFile {
  constructor(name, path, size, timeModified, type) {
    this.name = name
    this.path = path
    this.size = size
    this.timeModified = timeModified
    this.type = type
  }
}
